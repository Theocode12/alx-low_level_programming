#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of arguement passed
 * @argv: list of arguement passed
 * Return: return (0) if true
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
